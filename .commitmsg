harden security, improve UX, and update docs for production readiness

Backend:
- Lock judge override/accept/reject to reviewer/admin role
- Add org-scoped filtering to reviews (pending, team, update)
- Require authentication on task pack read endpoints
- Add request ID middleware, global error handler, rate limiting
- Add /health/ready readiness probe with DB connectivity check
- Configure structured logging with timestamps

Frontend:
- Add 401 interceptor that clears auth and redirects to /auth
- Consolidate 3 HTTP helpers (api, api-extensions, course-api) into one
- Replace silent error catches with user-facing toasts on dashboard
- Add colored sync status indicator (dot + descriptive label)
- Protect /course routes in middleware
- Add proper form labels, autoComplete, and aria-pressed to auth page
- Add skip-to-content link, aria-labels on icon buttons, sr-only class
- Add role="status" to loading spinner, role="alert" to error page
- Improve 404 page with Home + Dashboard links

Docs:
- Update README with full feature set (exams, certs, datasets,
  webhooks, audit, IAA, consensus, quality, course, SDK)
- Document all 19 API route groups and 21 Alembic migrations
